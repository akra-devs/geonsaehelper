/// Shared constants for the conversation feature (UI ↔︎ BLoC).
/// Keep values simple to avoid cross-layer coupling.
const String conversationUnknownValue = '__unknown__';

/// Last verified date for HUG rules used in UI outputs (YYYY-MM-DD).
/// Source of truth: docs/docs/RULES_HUG_mapping.yaml (keep in sync)
const String rulesLastVerifiedYmd = '2025-09-08';
